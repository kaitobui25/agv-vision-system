# Camera Capture Module

Simple camera capture module using OpenCV.

## Setup

```bash
pip install opencv-python
```

## Usage

```bash
python camera_server.py
```

Captures frames and saves to `/images/latest.jpg`
